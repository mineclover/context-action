name: Publish Tool Protocol Changelog Patch

on:
  workflow_dispatch:
    inputs:
      release_commit:
        description: 'Immutable main commit containing @context-action/tool-protocol@1.0.1'
        required: true
        type: string
      confirmation:
        description: 'Type publish-tool-protocol-1.0.1 to publish only the changelog patch to latest'
        required: true
        type: string

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish
  cancel-in-progress: false

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  TOOL_PROTOCOL_PACKAGE: '@context-action/tool-protocol'
  TOOL_PROTOCOL_VERSION: '1.0.1'

jobs:
  publish-tool-protocol-changelog-patch:
    runs-on: ubuntu-latest
    environment:
      name: npm-stable
    timeout-minutes: 30
    steps:
      - name: Checkout approved patch commit
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          ref: ${{ inputs.release_commit }}

      - name: Verify main, immutable commit, and explicit confirmation
        env:
          RELEASE_COMMIT: ${{ inputs.release_commit }}
          CONFIRMATION: ${{ inputs.confirmation }}
        run: |
          [[ "$RELEASE_COMMIT" =~ ^[0-9a-f]{40}$ ]]
          test "$GITHUB_REF" = "refs/heads/main"
          test "$(git rev-parse HEAD)" = "$RELEASE_COMMIT"
          git merge-base --is-ancestor "$RELEASE_COMMIT" origin/main
          test "$CONFIRMATION" = publish-tool-protocol-1.0.1

      - name: Clear npm config before registry setup
        run: |
          unset NPM_CONFIG_USERCONFIG
          rm -f ~/.npmrc

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: '24'
          registry-url: 'https://registry.npmjs.org'
          package-manager-cache: false

      - name: Setup pnpm
        uses: pnpm/action-setup@d15e628ca66d93ee5f352c71671a7bc6a97af5c9 # v6.0.0
        with:
          version: 10.30.3
          run_install: false

      - name: Verify npm automation-token auth
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: npm whoami

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate the exact documentation patch
        run: |
          node - <<'NODE'
          const pkg = require('./packages/tool-protocol/package.json');
          if (pkg.name !== process.env.TOOL_PROTOCOL_PACKAGE || pkg.version !== process.env.TOOL_PROTOCOL_VERSION) {
            throw new Error(`Expected ${process.env.TOOL_PROTOCOL_PACKAGE}@${process.env.TOOL_PROTOCOL_VERSION}`);
          }
          NODE
          pnpm verify:tool-protocol-changelog

      - name: Build and test Tool Protocol patch
        run: |
          pnpm --filter @context-action/tool-protocol build
          pnpm --filter @context-action/tool-protocol type-check
          pnpm --filter @context-action/tool-protocol test

      - name: Refuse an ambiguous existing patch version
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          if npm view "$TOOL_PROTOCOL_PACKAGE@$TOOL_PROTOCOL_VERSION" version --registry=https://registry.npmjs.org >/dev/null 2>&1; then
            latest="$(npm view "$TOOL_PROTOCOL_PACKAGE" dist-tags.latest --json --registry=https://registry.npmjs.org | tr -d '\"')"
            test "$latest" = "$TOOL_PROTOCOL_VERSION" || {
              echo 'Refusing to retag an already-published patch version; publish a new patch instead.' >&2
              exit 1
            }
            echo 'Tool Protocol changelog patch is already published and owns latest.'
            echo 'PUBLISH_REQUIRED=false' >> "$GITHUB_ENV"
          fi

      - name: Publish the new Tool Protocol patch to latest
        if: env.PUBLISH_REQUIRED != 'false'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: |
          pnpm publish:packages -- --summary-file reports/tool-protocol-changelog-patch-summary.json \
            --dist-tag latest --scope @context-action/tool-protocol

      - name: Verify latest replacement and capture evidence
        run: |
          node - <<'NODE'
          const { execFileSync } = require('node:child_process');
          const delay = new Int32Array(new SharedArrayBuffer(4));
          let tags;
          for (let attempt = 1; attempt <= 12; attempt += 1) {
            tags = JSON.parse(execFileSync('npm', ['view', process.env.TOOL_PROTOCOL_PACKAGE, 'dist-tags', '--json', '--registry=https://registry.npmjs.org'], { encoding: 'utf8' }));
            if (tags.latest === process.env.TOOL_PROTOCOL_VERSION) process.exit(0);
            if (attempt < 12) Atomics.wait(delay, 0, 0, 5_000);
          }
          throw new Error(`Tool Protocol latest tag did not converge after publication: ${JSON.stringify(tags)}`);
          NODE
          pnpm verify:published-tool-consumers -- --tag latest --packages "$TOOL_PROTOCOL_PACKAGE"
          pnpm capture:published-release -- --tag latest --packages "$TOOL_PROTOCOL_PACKAGE" \
            --consumer-status passed --output reports/tool-protocol-changelog-patch-registry-evidence.json

      - name: Upload changelog-patch evidence
        if: always()
        uses: actions/upload-artifact@bbbca2ddaa5d8feaa63e36b76fdaad77386f024f # v7.0.0
        with:
          name: tool-protocol-changelog-patch-evidence
          path: reports/tool-protocol-changelog-patch-*.json
          if-no-files-found: ignore
          retention-days: 30
