/**
 * Canonical Context-Action boundary for the context-store mouse comparison.
 *
 * The legacy `context/` module remains behind this export while its aggregate
 * compatibility helpers are migrated independently.
 */
export * from '../context/MouseEventsContext';
