# Evidence command logs are integrity-hashed raw process output. Preserve their
# exact bytes instead of reporting tool-emitted trailing spaces as source diffs.
release-evidence/**/logs/*.log -text -whitespace
